server {
    listen {{ item.listen }};
    server_name {{ item.server_name }};

    root {{ item.root }};

    index {{ item.index }};

    error_log /var/log/nginx/{{ item.server_name }}.{{ item.listen }}.log error;
}
